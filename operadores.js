class operadores{

    divisao(a,b){
        return a/b;
    }

    multiplicação (numUm, numDois) {
        return numUm * numDois;
    }

    soma(a, b) {
        return a + b;
    }

    subtracao(a, b){
        return a - b;
    }
}

module.exports = { operadores };