class Operadores{

    static divisao(a,b){
        return a/b;
    }

    static multiplicacao (numUm, numDois) {
        return numUm * numDois;
    }

    static soma(a, b) {
        return a + b;
    }

    static subtracao(a, b){
        return a - b;
    }
}

module.exports = { Operadores };